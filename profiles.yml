config:
  partial_parse: true
  printer_width: 120
  send_anonymous_usage_stats: false
  use_colors: true
normalize:
  outputs:
    prod:
      database: classicmodels_dest
      password: password
      port: 3306
      schema: classicmodels_dest
      server: localhost
      type: mysql
      username: root
  target: prod
